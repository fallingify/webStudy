day09
서브쿼리 -> 조인

1. 서브쿼리
	하나의 쿼리 내에 작성하는 또 다른 쿼리
	서브쿼리의 위치에 따라 종류를 구분한다
	서브쿼리는 메인 쿼리의 컬럼을 사용할 수 있지만, 메인 쿼리는 서브쿼리의 컬럼 사용 불가
	소괄호 안에 작성한다

1) FROM절 : IN LINE VIEW(인라인뷰)
	FROM 절 안에 또 다른 쿼리를 작성한다
	서브쿼리의 결과를 가상테이블처럼 사용한다

2) SELECT절 : SCALAR(스칼라 쿼리)
	SELECT 절 안에 또 다른 쿼리를 작성한다
	단일 컬럼, 단일 행을 반환한다(1개의 값으로 사용)

3) WHERE절 : SUB QUERY(서브쿼리)
	WHERE절이나 HAVING절 안에 또 다른 쿼리를 작성한다

연관 서브쿼리
	서브쿼리가 메인쿼리 컬럼을 가지고 있지 않는 경우
비연관 서브쿼리
	서브쿼리 메인쿼리 컬럼을 가지고 있는 경우

단일행 서브쿼리
	서브쿼리 실행 결과가 1행 이하(=, <, <=, >=, <> 연산자 사용)
다중행 서브쿼리
	서브쿼리 실행 결과가 여러 행(IN, ALL, ANY, SOME, EXISTS 연산자 사용)
	- IN : 조건 중 1개가 일치한다면
	- ALL : 서브쿼리 결과에 존재하는 모든 값을 만족하는 조건
	- ANY, SOME : 서브쿼리 결과에 존재하는 어느 하나의 값이라도 만족하는 조건
	- EXISTS : 서브쿼리의 결과를 만족하는 값이 존재하는지 여부 확인



SELECT		스칼라 서브쿼리
FROM		인라인 뷰
WHERE		중첩 서브쿼리
GROUP BY	
HAVING		중첩 서브쿼리
ORDER BY	서브쿼리


서브쿼리
	SELECT
	FROM
	WHERE
	GROUP BY
	HAVING
	ORDER BY

ROWNUM
	결과 행 앞에 1부터 1씩 증가하는 시퀀스를 붙여준다
	


































